# Core
NODE_ENV=development
APP_NAME=multi-tenant-saas-api
HOST=0.0.0.0
# PORT=7000

# Logging
LOG_LEVEL=info
LOG_DIR=logs

# CORS
CORS_ORIGIN=*

# Swagger
SWAGGER_ENABLED=true
SWAGGER_ROUTE_PREFIX=/docs

# Metrics
METRICS_ENABLED=true
METRICS_ROUTE=/metrics

# Rate limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_MAX=100
RATE_LIMIT_TIME_WINDOW=1 minute

# PostgreSQL (public schema / tenant registry)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=saas

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=2222

# RabbitMQ
RABBITMQ_URL=amqp://guest:guest@localhost:5672

# Tenancy
TENANT_HEADER_ID=x-tenant-id
TENANT_HEADER_KEY=x-tenant-key
# If set, tenant can be resolved from subdomain (e.g., acme.example.com where base domain is example.com)
TENANT_BASE_DOMAIN=

# System
# Used to protect system routes like POST /system/tenants
SYSTEM_API_KEY=BRMEwnWxvBQQ8iDxU1wC3Lpk7dGaIi39BHGJJn

# Auth
JWT_ACCESS_SECRET=ee39eb0993db583e198c05a295ab1c167e7d552ac8abb522b028d3e7d
JWT_ACCESS_TTL_SECONDS=900
REFRESH_TOKEN_TTL_SECONDS=2592000
SESSION_TTL_SECONDS=86400
